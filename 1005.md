```python
#coding=utf-8
user_input = raw_input()
my_list = []
my_list_st = []
result = []

num = int(user_input)
def solution(num):#将数字展开的函数
    counter = []
    while(num != 1):
        if num%2 != 0:
            num = (num*3+1)/2
            counter.append(num)
        else:
            num = num/2
            counter.append(num)
    return list(reversed(counter))

def solution_step(list1,list2):#比较两个列表包含关系的函数
    counter = 0
    if list1>list2:
        return 0
    else:
        for i in list1:
            if (not i in list2):
                break
            else:
                counter+=1
        if counter == len(list1):
            return 1
        else:
            return 0
for i in range(num):
    my_list.append(int(raw_input()))

temp = []
for i in my_list:
    if solution_step(temp,solution(i)) == 1:
        temp = solution(i)
        result.append(i)
        my_list.remove(i)
     

print result 
